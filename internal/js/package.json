{
  "devDependencies": {
    "jest": "^26.0.1"
  },
  "scripts": {
    "test": "jest",
    "clean": "rm -r dist && mkdir dist",
    "httpServer": "node website/uploadFile.js",
    "expressServer": "node website/index.js",
    "start-httpServer": "npm run httpServer",
    "prestart-httpServer": "npm install",
    "start-expressServer": "npm run expressServer",
    "prestart-expressServer": "npm install",
    "build-httpServer": "npm run start-httpServer",
    "build-expressServer": "npm run start-expressServer"
  },
  "dependencies": {
    "ejs": "^3.1.3",
    "express": "^4.17.1",
    "formidable": "^1.2.2",
    "http": "0.0.1-security",
    "multer": "^1.4.2",
    "guid": "0.0.12",
    "mime": "^2.4.6",
    "path": "^0.12.7",
    "util": "^0.12.3"
  }
}
